<!DOCTYPE html>
<html>
<head>
  <title>Foto Rahasia</title>
  <style>
    /* Ganti dengan URL gambar Anda */
    .secret-image {
      background-image: url('');
      width: 400px;
      height: 300px;
      background-size: cover;
      position: relative;
      justify-content: center;
      align-items: center;
      display: flex;
    }

    /* Gaya overlay */
    .overlay {
      position: absolute;
      justify-content: center;
      align-items: center;
      display: flex;
      width: 100%;
      height: 100%;
      pointer-events: none; /* Memastikan overlay tidak mengganggu klik atau seleksi teks */
    }
  </style>
</head>
<body>
  <div class="secret-image" oncontextmenu="return false;">
    <div class="overlay"></div>
  </div>
  <script>
    // Mencegah tombol kanan mouse
    window.addEventListener('contextmenu', function (e) {
      e.preventDefault();
    });
  </script>
</body>
</html>
